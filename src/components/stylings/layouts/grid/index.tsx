import { Container, Footer, Header, Main } from './styles';

export const Grid: React.VFC = () => {
  return (
    <Container>
      <Header />
      <Main />
      <Footer />
    </Container>
  );
};
